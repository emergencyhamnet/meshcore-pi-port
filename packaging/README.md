# Packaging Notes

This directory holds Pi-specific runtime packaging only.

It may contain:

1. systemd unit files
2. environment templates
3. release packaging metadata

It must not contain gateway-specific runtime features.